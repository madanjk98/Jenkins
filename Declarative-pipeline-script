pipeline {
    agent any
    stages {
        stage('Git checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/madanjk98/Jenkins.git'
            }
        }
        stage('mvn build') {
            steps {
                sh "mvn clean package"
            }
        }
    }
}
